<view class="page full-size">
	<map scale="{{scale}}" longitude="{{location.lng}}" latitude="{{location.lat}}" markers="{{markers}}" controls="{{controls}}" bindcontroltap="controltap" show-location style="width: 100%; height: 100%;"></map>
</view>